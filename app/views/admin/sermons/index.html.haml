%h1 Listing sermons

.navbar
  .navbar-inner
    %ul.nav
      %li.divider-vertical
      %li
        = link_to 'New Sermon', new_admin_sermon_path
      %li.divider-vertical

%table.table.table-bordered
  %tr
    %th Date
    %th Title
    %th Summary
    %th

  - @sermons.each do |sermon|
    %tr
      %td= sermon.date
      %td= sermon.title
      %td= sermon.summary
      %td 
        = link_to 'Show', admin_sermon_path(sermon), class: 'btn'
        = link_to 'Edit', edit_admin_sermon_path(sermon), class: 'btn'
        = link_to 'Destroy', admin_sermon_path(sermon), :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn'

