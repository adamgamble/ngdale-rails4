%article#content.tabs
  .wrapper
    .box1
      .wrapper
        %section.col4
          %h2
            %strong>= @page.name[0]
            =@page.name[1..-1]
          %div
            %figure.left.marg_right1
              %img#image{:style => "width: 250px; height: 180px;", :alt => "", :src => @page.image.url(:small)}/
            #body=@page.body.try(:html_safe)
